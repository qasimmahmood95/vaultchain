# pnpm 11+: build-script allowlist lives here, not in package.json.
# Prisma needs postinstall for engine/client generation; esbuild ships a native binary (tsx dep).
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  esbuild: true
  prisma: true
